# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
wheels/
*.whl

# Virtual environments
.venv/
venv/
ENV/

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Testing / Coverage
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.tox/
.nox/

# Environment / Secrets
.env
.env.*
# ...except the committed template. Keeping .env.example tracked so
# first-time setup (and snapshot packaging) has a documented schema.
!.env.example

# Distribution / Packaging
*.tar.gz
*.zip

# Ruff / Linting cache
.ruff_cache/

# ANTLR generated (keep committed, but ignore intermediates)
*.class
*.jar

# Misc
*.log
ui/node_modules/
ui/dist/

# Local runtime state (corrections learning SQLite DB + WAL)
corrections.db
corrections.db-shm
corrections.db-wal
nl_corrections.db
nl_corrections.db-shm
nl_corrections.db-wal

# NL->Cypher eval harness run artifacts (WP-25.5).
# Each refresh produces tests/nl2cypher/eval/reports/<date>-<config>.{json,md}.
# The committed `baseline.json` is the gate's reference; per-run reports
# stay local (or get uploaded as CI artifacts).
tests/nl2cypher/eval/reports/
